<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task.id)%> - <%= link_to "delete",
  task_path(task.id),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%><br>
<% end %>
<br>
<%= link_to "Create a task", new_task_path %>
