<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <input type="checkbox"><%= link_to task.title, task_path(task.id)%> - <%= link_to "delete",
  task_path(task.id),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%><br>
<% end %>
<br>
<%= link_to "Create a task", tasks_new_path %>
