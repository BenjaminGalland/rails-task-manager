<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<% if @task.completed %>
  <p>This task has been completed.</p>
<% else %>
  <p>This task hasn't been completed yet.</p>
<% end %>
<%= link_to "Back to list", tasks_path %>
<%= link_to "Edit the task", edit_task_path(@task.id)%>
